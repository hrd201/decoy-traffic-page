- 用户明确偏好：出现高频 fetch failed 时，优先使用官方重置命令恢复基线：`curl -fsSL https://openclaw.ai/install.sh | bash`。
- 关键排障结论：曾存在 `openclaw.service` 与 `openclaw-gateway.service` 并存导致端口冲突；已将 `openclaw.service` 处理为 masked，仅保留 `openclaw-gateway.service`。
- 用户要求：日志监测改为“有错误才通知、无错误不打扰”，并在后续要求下停用了 `openclaw:log-error-watch`（需要时可再启用）。
- 用户长期需求：维护 skills 到 GitHub 仓库并可恢复；已建立 `openclaw-skills-hub` 与 `windows-oauth-link-proxy`。
- 用户偏好：搜索默认走浏览器 + Google，遇阻断走合规后备源；不要提供验证码绕过方案。
